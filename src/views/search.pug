extends base
include mixins/video

block content 
    form(method="GET").search__form
        input(placeholder="Serach by title", type="text", name="keyword")
        button 
            i.fas.fa-search
    div.video-grid
        each video in videos 
            +video(video)
        else
            span.empty__message Video is not found