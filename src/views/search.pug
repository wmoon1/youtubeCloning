extends base
include mixins/video

block content 
    form(method="GET")
        input(placeholder="Serach by title", type="text", name="keyword")
        input(type="submit", value="Search Now")
    div
        each video in videos 
            +video(video)